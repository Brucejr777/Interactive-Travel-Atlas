# Interactive Travel Atlas

An interactive atlas of countries, cultures, landmarks, foods, and people.
Built with **React 19**, **Vite 8**, **TypeScript 6**, **react-simple-maps**,
**zustand**, and **world-atlas** TopoJSON data.

## Getting started

```bash
cd atlas-app
npm install
npm run dev